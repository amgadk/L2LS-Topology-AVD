---
# inventory.yml
DC1:
  children:
    DC1_FABRIC:
      vars:
        ansible_user: arista
        ansible_ssh_pass: "Arista123!"
# Use this section if you want to perform testing via the eAPI
        ansible_connection: httpapi
        ansible_httpapi_use_ssl: True
        ansible_httpapi_validate_certs: False
        ansible_network_os: eos
        ansible_httpapi_port: 443
        ansible_become: yes
        ansible_become_method: enable
# Use this section if you want to perform testing via the CLI through SSH
        # ansible_connection: network_cli
        # ansible_network_os: eos
        # ansible_become: yes
        # ansible_become_method: enable
      children:
        DC1_SPINES:
          hosts:
            spine1:
              ansible_host: 10.18.136.228
            spine2:
              ansible_host: 10.18.137.91
        DC1_LEAFS:
          hosts:
            pod-4-1:
              ansible_host: 10.18.136.173
    DC1_NETWORK_SERVICES:
      children:
        DC1_LEAFS:
        DC1_SPINES:
    DC1_ENDPOINT_CONNECT:
      children:
        DC1_LEAFS:
        DC1_SPINES:

